apiVersion: v2
name: cf-zitadel-access-operator
description: A Kubernetes operator that provisions Zitadel OIDC apps and Cloudflare Access policies for SecuredApplication resources.
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: cloudflare-tunnel-ingress-controller
    version: "0.0.22"
    repository: "https://helm.strrl.dev"
    condition: cloudflare-tunnel-ingress-controller.enabled
